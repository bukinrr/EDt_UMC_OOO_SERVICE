#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	ТЗСтрокиГрафика = ГрафикПроцедур.Выгрузить();
	ТЗСтрокиГрафика.Свернуть("Процедура, Время");
	
	Для Каждого СтрокаГрафика Из ТЗСтрокиГрафика Цикл
		
		Если ГрафикПроцедур.НайтиСтроки(Новый Структура("Процедура, Время", СтрокаГрафика.Процедура, СтрокаГрафика.Время)).Количество() > 1 Тогда 
			ОбщегоНазначения.СообщитьОбОшибке(
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru ='Процедура ""%1"" и время ""%2"" указаны дважды (ошибка)'"),
			СтрокаГрафика.Процедура,
			Формат(СтрокаГрафика.Время, "ДФ=HH:mm")), Отказ);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти