#Область ОбработчикиКомандФормы
&НаКлиенте
Процедура Дополнить(Команда)
	ДополнитьСервер();	
КонецПроцедуры

&НаСервере
Процедура ДополнитьСервер()
	ВнОбъект = ДанныеФормыВЗначение(Объект, Тип("СправочникОбъект.ГруппыОбъектовМетаданных"));
	ВнОбъект.Дополнить();
	ЗначениеВРеквизитФормы(ВнОбъект, "Объект");
	Модифицированность = Истина;
КонецПроцедуры

&НаКлиенте
Процедура КомандаВыделитьВсеМетаданные(Команда)
	
	Для Каждого СтрокаСостава Из Объект.Состав Цикл
		СтрокаСостава.Выгружать = Истина;	
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаСнятьВедылениеВсеМетаданные(Команда)
	
	Для Каждого СтрокаСостава Из Объект.Состав Цикл
		СтрокаСостава.Выгружать = Ложь;	
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти