#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если Не ЭтоГруппа Тогда
		// Дополнительная нормализация шаблона.
		Если Не ПустаяСтрока(ЭтотОбъект.ТекстHTML) И 
			ДополнительныеСвойства.Свойство("НормализоватьШаблон") 
		Тогда
			ЭтотОбъект.ТекстHTML = РаботаСШаблонамиHTMLКлиентСервер.НормализоватьТекстHTMLШаблона(ЭтотОбъект.ТекстHTML);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
#КонецОбласти
