#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если Не ЗначениеЗаполнено(ЭтотОбъект.Пользователь) Тогда 
		ЭтотОбъект.Пользователь = ПараметрыСеанса.ТекущийПользователь;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ЭтотОбъект.НастройкаРасположенияОтборов) Тогда 
		ЭтотОбъект.НастройкаРасположенияОтборов = Перечисления.ВидыНастроекРасположенияОтборовФормыПодбораКлиентов.НаНесколькихСтраницах;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти