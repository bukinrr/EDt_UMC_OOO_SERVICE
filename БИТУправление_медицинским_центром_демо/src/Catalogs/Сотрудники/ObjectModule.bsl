#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если Не ЭтоГруппа Тогда
		ФИО = ОтраслевыеДополнения.КонтрольКорректностиФИО(Наименование,Фамилия, Имя, Отчество);
		Фамилия		= ФИО["Фамилия"];
		Имя			= ФИО["Имя"];
		Отчество	= ФИО["Отчество"];
		Наименование= ФИО["Наименование"];
	КонецЕсли;
	
	Если Не ЭтоГруппа И Специализации.Найти(Специализация) = Неопределено Тогда
		Специализация = Неопределено;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти