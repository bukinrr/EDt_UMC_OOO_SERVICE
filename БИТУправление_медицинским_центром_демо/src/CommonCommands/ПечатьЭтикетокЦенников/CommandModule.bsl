#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если ПараметрКоманды <> Неопределено Тогда
		
		Парам = Новый Структура;
		
		Парам.Вставить("ОткрытиеИзДокументов",ПараметрКоманды);
		
		ФормаОбработки = ПолучитьФорму("Обработка.ПечатьЭтикетокИЦенников.Форма.ФормаУпр",Парам);
		Если Не ФормаОбработки.Открыта() Тогда
			ФормаОбработки.Открыть();
		Иначе
			ФормаОбработки.Активизировать();
		КонецЕсли;
		Оповестить("ПечатьЭтикетокИЦенников_ЗаполнитьТаблицу", ПараметрКоманды);
		
	КонецЕсли;	
	
КонецПроцедуры

#КонецОбласти


