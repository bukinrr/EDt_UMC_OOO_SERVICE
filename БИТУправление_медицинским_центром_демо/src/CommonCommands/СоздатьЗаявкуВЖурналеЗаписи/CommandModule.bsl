#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	СообщениеОбОшибке = "";	
	ПараметрыЗаписи = УправлениеЗаявками.ПолучитьПараметрыЗаписиПоЛистуОжидания(ПараметрКоманды, СообщениеОбОшибке);	
	Если ПараметрыЗаписи = Неопределено Тогда
		Если Не ПустаяСтрока(СообщениеОбОшибке) Тогда 
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(СообщениеОбОшибке);
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	КалендарьПланированияКлиент.ОткрытьАктивизироватьКалендарьПланирования();
	Оповестить("НамерениеЗаписиПоДокументу", ПараметрыЗаписи, ПараметрыЗаписи.ДокументЗаписи);
	
КонецПроцедуры

#КонецОбласти
