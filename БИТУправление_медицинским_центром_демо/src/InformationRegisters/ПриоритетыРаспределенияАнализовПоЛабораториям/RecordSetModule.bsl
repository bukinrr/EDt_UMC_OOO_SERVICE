#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)
	
	Для Каждого ТекущаяЗапись Из ЭтотОбъект Цикл
		
		Запись = РегистрыСведений.ПриоритетыРаспределенияАнализовПоЛабораториям.СоздатьМенеджерЗаписи();
		Запись.Лаборатория = ТекущаяЗапись.Лаборатория;
		Запись.Номенклатура = ТекущаяЗапись.Номенклатура;
		Запись.Филиал = ТекущаяЗапись.Филиал;
		Запись.Прочитать();
		Запись.Удалить();
		
	КонецЦикла;
	
КонецПроцедуры
#КонецОбласти
