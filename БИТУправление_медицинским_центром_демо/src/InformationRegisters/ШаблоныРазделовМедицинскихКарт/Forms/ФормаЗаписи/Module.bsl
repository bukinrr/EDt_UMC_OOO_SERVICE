#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Запись.Раздел)
		И Не ЗначениеЗаполнено(Запись.ШаблонПриема)
	Тогда
		ТекущийЭлемент = Элементы.ШаблонПриема;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
