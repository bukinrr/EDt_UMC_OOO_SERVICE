#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если Не ЗначениеЗаполнено(Параметры.Ключ) Тогда
		Запись.АдресПодключения = "api.gemotest.ru";
		Запись.СоздаватьНовуюНоменклатуру = Истина;
		Запись.ОбновлятьРеквизитыНоменклатурыПриПовторнойЗагрузке = Истина;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
