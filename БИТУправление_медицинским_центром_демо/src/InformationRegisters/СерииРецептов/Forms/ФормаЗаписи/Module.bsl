#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ОткрытьФорму("РегистрСведений.СерииРецептов.Форма.УстановкаСерийРецепта", 
				Новый Структура("Филиал", Запись.Филиал),
				ЭтаФорма.ВладелецФормы,,,,,
				?(ЭтаФорма.ВладелецФормы = Неопределено, Неопределено, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца));
	Закрыть();
КонецПроцедуры

#КонецОбласти
