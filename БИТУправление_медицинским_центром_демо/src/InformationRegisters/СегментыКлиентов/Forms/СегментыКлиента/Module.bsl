
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Клиент = Параметры.Клиент;
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	УстановитьОтборСреза();
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура КлиентПриИзменении(Элемент)
	УстановитьОтборСреза();
КонецПроцедуры

&НаКлиенте
Процедура СрезПоследнихПриАктивизацииСтроки(Элемент)
	УстановитьОтборИсторииВидаСегментации();
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура УстановитьОтборСреза()
	РаботаСФормамиКлиент.УстановитьОтборСписка("Клиент", Клиент, СрезПоследних);
	УстановитьОтборИсторииВидаСегментации();
КонецПроцедуры

&НаКлиенте
Процедура УстановитьОтборИсторииВидаСегментации()
	
	ТекущиеДанныеСреза = Элементы.СрезПоследних.ТекущиеДанные;
	Если ТекущиеДанныеСреза = Неопределено Тогда
		ВидСегментации = ПредопределенноеЗначение("Справочник.ВидыСегментации.ПустаяСсылка");
	Иначе
		ВидСегментации = ТекущиеДанныеСреза.ВидСегментации;
	КонецЕсли;
	
	РаботаСФормамиКлиент.УстановитьОтборСписка("Клиент", Клиент, ИсторияСегмента);
	РаботаСФормамиКлиент.УстановитьОтборСписка("ВидСегментации", ВидСегментации, ИсторияСегмента);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьВсе(Команда)
	Элементы.СрезПоследних.Обновить();
	Элементы.ИсторияСегмента.Обновить();
КонецПроцедуры

#КонецОбласти