Процедура ПередЗаписью(Отказ, Замещение)
	
	нзСписанныеТовары = РегистрыСведений.СписанныеТовары.СоздатьНаборЗаписей();
	нзСписанныеТовары.Отбор.Регистратор.Установить(Регистратор);
	нзСписанныеТовары.Прочитать();
	Если нзСписанныеТовары.Количество()=0 Тогда
		Очистить();
	КонецЕсли
КонецПроцедуры
