#Область ПрограммныйИнтерфейс

// Имя поля для представления пользователю по имени в конфигураторе.
//
// Параметры:
//  ИмяРеквизита - Строка	 - имя поля в конфигураторе.
// 
// Возвращаемое значение:
//  Строка.
//
Функция ПолучитьСпециализированноеИмяРеквизита(ИмяРеквизита) Экспорт
	
	Если ИмяРеквизита = "Филиал" Тогда
		Возврат "Клиника";
		
	ИначеЕсли ИмяРеквизита = "Сотрудник" Тогда
		Возврат "Врач";
		
	ИначеЕсли ИмяРеквизита = "Клиент" Тогда
		Возврат "Пациент";
		
	Иначе
		Возврат ИмяРеквизита;
	КонецЕсли;
	
КонецФункции

#КонецОбласти