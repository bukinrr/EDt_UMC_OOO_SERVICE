#Область ПрограммныйИнтерфейс

// Возвращает булево значение, соответствующее наличию в конфигурации справочника "СтраховыеПолисы".
// 
// Возвращаемое значение:
//   Булево.
//
Функция ИспользуютсяПолисы() Экспорт
	Если Метаданные.Справочники.Найти("СтраховыеПолисы") = Неопределено Тогда
		Возврат Ложь;
	Иначе
		Возврат Истина;	
	КонецЕсли;
КонецФункции

// Возвращает булево значение, соответствующее наличию в табличной части диагнозов документа Прием данных о зубе, 
// сегменте зуба.
// 
// Возвращаемое значение:
//   Булево.
//
Функция ЕстьЗубыВДиагнозахПриемов() Экспорт

	Возврат Метаданные.Документы.Прием.ТабличныеЧасти.Диагнозы.Реквизиты.Найти("ЗубВКарте") <> Неопределено;
	
КонецФункции

// Группы состояния здоровья в виде номера и представления.
// 
// Возвращаемое значение:
//  СписокЗначений.
//
Функция ПолучитьГруппыСостоянияЗдоровья() Экспорт
	
	СписокГруппСостоянияЗдоровья = Новый СписокЗначений;
	СписокГруппСостоянияЗдоровья.Добавить(1, "I группа");
	СписокГруппСостоянияЗдоровья.Добавить(2, "II группа");
	СписокГруппСостоянияЗдоровья.Добавить(3, "III группа");
	СписокГруппСостоянияЗдоровья.Добавить(4, "IV группа");
	СписокГруппСостоянияЗдоровья.Добавить(5, "V группа");
	
	Возврат СписокГруппСостоянияЗдоровья;
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Все варианты значений классификации диагнозов по виду нозологической единицы (по мотивам 1.2.643.5.1.13.13.11.1077).
// 
// Возвращаемое значение:
//  Массив - из Перечисления.КлассификацииДиагнозов.
//
Функция ВидыНозологий() Экспорт
	
	пчВидыНозологии = Перечисления.КлассификацииДиагнозов;
	ВидыНозологий = Новый Массив;
	ВидыНозологий.Добавить(пчВидыНозологии.ОсновноеЗаболевание);
	ВидыНозологий.Добавить(пчВидыНозологии.ОсложнениеОсновногоЗаболевания);
	ВидыНозологий.Добавить(пчВидыНозологии.СопутствующееЗаболевание);
	ВидыНозологий.Добавить(пчВидыНозологии.ВнешняяПричина);
	ВидыНозологий.Добавить(пчВидыНозологии.КонкурирующееЗаболевание);
	ВидыНозологий.Добавить(пчВидыНозологии.ФоновоеЗаболевание);
	ВидыНозологий.Добавить(пчВидыНозологии.ОсложнениеСопутствующегоЗаболевания);
	
	Возврат ВидыНозологий;
	
КонецФункции

// См. Обработки.ИнтерфейсВводаПолейЭМД.СвязиНазначенийПараметровДляВвода().
//
Функция СвязиНазначенийПараметровШаблонов() Экспорт
	
	Возврат Обработки.ИнтерфейсВводаПолейЭМД.СвязиНазначенийПараметровДляВвода();
	
КонецФункции

#КонецОбласти
