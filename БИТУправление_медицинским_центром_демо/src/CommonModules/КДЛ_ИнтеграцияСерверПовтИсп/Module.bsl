#Область ПрограммныйИнтерфейс

// Стандартные сообщения об ошибках у данной интеграции.
//
// Параметры:
//  ПараметрыСообщения	 - неопределено - не используется
// 
// Возвращаемое значение:
//  Структура - сообщения об ошибках в формате Ключ = Ошибка, Значение = Представление ошибки.
//
Функция СообщенияОбОшибках(ПараметрыСообщения = Неопределено) Экспорт
	
	ВозвращаемоеЗначение = Новый Структура;
	
	ВозвращаемоеЗначение.Вставить("ОтключенаИнтеграция", НСтр("ru='Не включена интеграция с лабораториями КДЛ в настройках медицинского учета!'"));

	ВозвращаемоеЗначение.Вставить("ОтсутствуетНастройка", НСтр("ru='Не заполнена или отключена настройка взаимодействия с лабораторией!'"));
	
	ВозвращаемоеЗначение.Вставить("ПустойПараметрПодключения", НСтр("ru='Не все параметры подключения заполнены в настройке взаимодействия с лабораторией!'"));

	ВозвращаемоеЗначение.Вставить("ОшибкаПодключения", НСтр("ru='Не удалось установить соединение с текущими параметрами настройки взаимодействия!'"));
	
	ВозвращаемоеЗначение.Вставить("ОшибкаКаталог", НСтр("ru='Не удалось установить текущий каталог!'"));
	
	ВозвращаемоеЗначение.Вставить("ОшибкаЧтенияФайла", НСтр("ru='Не удалось прочитать файл справочника!'"));
	
	Возврат ВозвращаемоеЗначение;
	
КонецФункции

#КонецОбласти