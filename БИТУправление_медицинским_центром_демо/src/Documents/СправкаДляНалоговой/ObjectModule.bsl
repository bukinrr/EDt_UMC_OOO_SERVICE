#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Налогоплательщик = СокрЛП(Налогоплательщик);
	
	Если ДатаВыдачи < Дата(2024,1,1)
		И ЗначениеЗаполнено(НомерСправки + НомерКорректировки)
	Тогда
		НомерСправки = 0;
		НомерКорректировки = 0;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти