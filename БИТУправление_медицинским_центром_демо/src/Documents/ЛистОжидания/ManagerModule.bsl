#Область ОбработчикиСобытий

Процедура ОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка)
	
	Если ВидФормы = "ФормаОбъекта" И Параметры.Свойство("Основание") И
		ЗначениеЗаполнено(Параметры.Основание) И
		Метаданные.ОпределяемыеТипы.ОснованиеЛистаОжидания.Тип.СодержитТип(ТипЗнч(Параметры.Основание))
	Тогда
		СтандартнаяОбработка = Ложь;
		ВыбраннаяФорма = "ФормаБыстрогоСоздания";
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти