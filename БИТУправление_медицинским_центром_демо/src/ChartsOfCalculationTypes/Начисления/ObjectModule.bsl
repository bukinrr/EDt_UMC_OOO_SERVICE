#Область ОбработчикиСобытий
Процедура ПередЗаписью(Отказ)
	
	Если Не ЗначениеЗаполнено(Периодичность) Тогда
		Периодичность = Перечисления.ПериодичностиВидовРасчета.Месяц;
	КонецЕсли;
	
	ПоказателиГрафика = Новый Массив;
	ПоказателиГрафика.Добавить(Перечисления.ПоказателиРасчетаНачислений.ДнейПоНорме);
	ПоказателиГрафика.Добавить(Перечисления.ПоказателиРасчетаНачислений.ДнейОтработаноПоНорме);
	ПоказателиГрафика.Добавить(Перечисления.ПоказателиРасчетаНачислений.ДнейОтработаноСверхНормы);
	ПоказателиГрафика.Добавить(Перечисления.ПоказателиРасчетаНачислений.ЧасовПоНорме);
	ПоказателиГрафика.Добавить(Перечисления.ПоказателиРасчетаНачислений.ЧасовОтработаноПоНорме);
	ПоказателиГрафика.Добавить(Перечисления.ПоказателиРасчетаНачислений.ЧасовОтработаноСверхНормы);
	
	Если (ПоказателиГрафика.Найти(ПоказательРасчета1) <> Неопределено ИЛИ
			ПоказателиГрафика.Найти(ПоказательРасчета2) <> Неопределено ИЛИ
			ПоказателиГрафика.Найти(ПоказательРасчета3) <> Неопределено)
		И Периодичность <> Перечисления.ПериодичностиВидовРасчета.Месяц
	Тогда
	    ТекстСообщения = НСтр("ru='Не допускается использование видов начислений, использующих данные графика работы, с периодичностью отличной от ""Месяц""!'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения,,,,Отказ);
		Возврат;
	КонецЕсли;

КонецПроцедуры
#КонецОбласти
