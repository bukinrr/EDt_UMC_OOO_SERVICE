#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	
	Если ЭтоГруппа Или ОбменДанными.Загрузка Тогда 
		Возврат;
	КонецЕсли;
	
	// Проверка корректности имени параметра.
	Попытка
		Структура = Новый Структура(ЭтотОбъект.Наименование);
	Исключение
		Отказ = Истина;
		ТекстОшибки = НСтр("ru = 'Неверное имя! Имя должно состоять из одного слова, начинаться с буквы и не содержать специальных символов кроме ""_"".'");
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки, ЭтотОбъект, "Наименование");
	КонецПопытки;
	
	Если ПустаяСтрока(ЭтотОбъект.Заголовок) Тогда 
		ЭтотОбъект.Заголовок = ЭтотОбъект.Наименование;
	КонецЕсли;
	
КонецПроцедуры
#КонецОбласти
