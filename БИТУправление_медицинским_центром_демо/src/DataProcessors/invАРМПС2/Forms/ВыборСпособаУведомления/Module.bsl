#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПродолжитьОформление(Команда)
	СписокЗначений = Новый СписокЗначений;
	Если ЗначениеЗаполнено(ЭлектроннаяПочта) Тогда
		СписокЗначений.Добавить(ЭлектроннаяПочта,"Email");
	КонецЕсли;
	Если ЗначениеЗаполнено(Телефон) Тогда
		СписокЗначений.Добавить(Телефон,"SmsNotification");
	КонецЕсли;
	//Если ЗначениеЗаполнено(СписокЗначений) Тогда
		Закрыть(СписокЗначений);
	//КонецЕсли;
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийФормы
 
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Клиент = Параметры.Клиент;
	ЭлектроннаяПочта = КонтактнаяИнформацияСервер.ПолучитьКИОбъекта(Клиент, Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты,, Истина);
	Телефон = КонтактнаяИнформацияСервер.ПолучитьКИОбъекта(Клиент,Перечисления.ТипыКонтактнойИнформации.Телефон,,Истина);
КонецПроцедуры

#КонецОбласти
